---
name: Backend Models
description: "Define database models and ORM entities with proper naming, data types, constraints, and relationships. Use this skill when: (1) Creating new model or entity classes, (2) Working on files in /models/, /entities/, /schemas/, or /orm/ directories, (3) Defining database table structures and column types, (4) Adding foreign keys, relationships, or associations between models, (5) Implementing model validations and constraints, (6) Adding timestamps (created_at, updated_at) to tables, (7) Working with Pydantic models, SQLAlchemy models, Django models, ActiveRecord, Prisma schema, or TypeORM entities, (8) Defining indexes on frequently queried columns, (9) Reviewing or refactoring existing model definitions."
---

# Backend Models

## When to use this skill:

- When creating new model or entity classes
- When working on files in `/models/`, `/entities/`, `/schemas/`, or `/orm/` directories
- When defining database table structures and column types
- When adding foreign keys, relationships, or associations between models
- When implementing model validations and constraints (NOT NULL, UNIQUE, CHECK)
- When adding timestamps (created_at, updated_at) to tables
- When working with Pydantic models, SQLAlchemy, Django ORM, ActiveRecord, Prisma, or TypeORM
- When defining indexes on frequently queried columns or foreign keys
- When reviewing or refactoring existing model definitions
- When designing cascade behaviors for relationships

## Instructions

For details, refer to the information provided in this file:
[backend models](../../../agent-os/standards/backend/models.md)